#include <stdio.h>

int diametro (float raio) {
    float resultado = raio * 2;
    printf("O diâmetro do circulo é: %.2fcm", resultado);
}  

int main (void) {
    float raio;
    printf("Insira o raio do círculo em cm: ");
    scanf("%f", &raio);
    diametro(raio);
    return 0;
}